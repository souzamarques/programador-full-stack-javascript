let velocidade = 50

while(velocidade <= 100) {
    console.log("Acelerando estamos a: " + velocidade + "km/s")
    velocidade++
}

velocidade = 80

while(velocidade > 0) {
    console.log("Desacelerando estamos a: " + velocidade + "km/s")
    velocidade--
}